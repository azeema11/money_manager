name: money_manager
version: 1.0.0+1
publish_to: none
description: A new Flutter project.
environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  get: 4.1.4
  moor_flutter: ^4.0.0

dev_dependencies:
  build_runner: ^2.0.4
  moor_generator: ^4.3.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
